{
  "name": "tutorias-functions",
  "private": true,
  "engines": {
    "node": "^20"
  },
  "main": "index.js",
  "scripts": {
    "build": "echo 'no build step'",
    "lint": "echo 'no lint'",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.0.0"
  }
}

